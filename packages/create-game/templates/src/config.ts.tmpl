import type { GameConfig } from "@littlepartytime/sdk";

const config: GameConfig = {
  id: "{{gameId}}",
  name: "{{gameName}}",
  description: "{{description}}",
  coverImage: "/games/{{gameId}}/cover.png",
  minPlayers: {{minPlayers}},
  maxPlayers: {{maxPlayers}},
  tags: [{{tags}}],
  version: "1.0.0",
  sdkVersion: "1.0.0",
};

export default config;
